import React from 'react';

const SaveChanges = () => {
    return (
        <button type='submit' className='saveChanges'>
            Сохранить изменения
        </button>
    );
};

export default SaveChanges;
